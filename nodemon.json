{
  "watch": [
    "src"
  ],
  "ext": "ts",
  "ignore": [],
  "exec": "sudo hb-service restart && npm run build && sudo hb-service logs",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
